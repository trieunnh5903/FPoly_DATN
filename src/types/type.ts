import { styled } from "nativewind";
import { Image } from "react-native";
import { Button, Text, View } from "react-native";
import { ScrollView, TextInput, TouchableOpacity } from "react-native-gesture-handler";

export const TextTw = styled(Text);
export const TextInputTw = styled(TextInput);
export const ViewTw = styled(View);
export const ButtonTw = styled(Button);
export const TouchableOpacityTw = styled(TouchableOpacity);
export const ImageTw = styled(Image);
export const ScrollViewTw = styled(ScrollView);
